<h1><%= @character_notes.name %></h1>

<%= render @character_notes %>

<div class="actions">
  <%= link_to 'Edit', edit_character_notes_path(@character_notes) %>
  <%= link_to 'Back', character_noteses_path %>
</div>
